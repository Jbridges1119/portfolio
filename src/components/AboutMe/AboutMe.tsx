import { Box } from '@mui/system'
import React from 'react'

const AboutMe = () => {
  return (
    <Box sx={{height: '800px', zIndex: 10, background: `white`, position: 'relative', width:'100%',}}>About Me</Box>
  )
}

export default AboutMe